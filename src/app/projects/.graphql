query {
  user(login: "nom_utilisateur") {
    repositories(first: 10) {
      nodes {
        name
        description
        language
        forkCount
        stargazerCount
        url
      }
    }
  }
}